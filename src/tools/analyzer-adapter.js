/**
 * Shared analyzer result helpers (no base-class port).
 * Wrappers may call createAnalyzerResult directly.
 */
export {
  ANALYZER_STATUSES,
  assertSafeAnalyzerOutputDirectory,
  createAnalyzerResult,
} from "./analyzer-result.js";
