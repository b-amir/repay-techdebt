## What you will learn

You will learn how capture settles an order before money moves. This matters because a broken settle path can drop funds silently. Keep the lesson on one handoff.

## Follow capture

The entry in `src/missing-file.ts:99` pretends to start capture. That citation is intentionally invalid so the evidence floor fails.

The helper in `src/also-missing.ts:1` does not exist either. Use this draft only as a negative fixture.

## Why the boundary matters

A valid lesson would cite real files and explain the settle contract. This draft is poisoned on purpose.

## Change it safely

Do not save this lesson. Fix citations against the live target first, then rewrite.

## Try it

Name the real capture file you would open instead of the fake paths above.
